services:
  workspace-hub:
    build: .
    image: mcp/workspace-hub
    container_name: workspace-hub
    stdin_open: true
    volumes:
      - ./workspaces:/app/workspaces
    restart: unless-stopped
