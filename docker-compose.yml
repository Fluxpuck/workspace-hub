services:
  workspace-hub:
    build: .
    image: mcp/workspace-hub
    container_name: workspace-hub
    ports:
      - "4440:4440"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
